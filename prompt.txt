# Car Rental Website Project - Complete Context

## Project Overview
I'm building a full-stack car rental website called "Sihabi Cars" for a Moroccan car rental company. The tech stack is:
- **Frontend**: React + TypeScript + Vite + Tailwind CSS + shadcn/ui + Framer Motion
- **Backend**: Node.js + Express + SQLite
- **Email**: Nodemailer with Gmail SMTP
- **Multi-language**: French (default), Arabic, English

## Project Structure

wheelie-happy-car/
├── src/ # Frontend React app
│ ├── components/ # Reusable components
│ ├── pages/ # Page components
│ ├── hooks/ # Custom hooks (auth, language, theme)
│ └── lib/ # API helpers and utilities
├── server/ # Backend Node.js server
│ ├── routes/ # API routes
│ ├── services/ # Email service, scheduler
│ ├── middleware/ # Auth middleware
│ ├── db.js # Database initialization
│ └── index.js # Server entry point
└── server/wheelie.db # SQLite database



## Database Schema (SQLite)

### users table
- id, email, password (bcrypt hashed)
- first_name, last_name, phone, city
- cin, driver_license (newly added)
- role (user/admin), created_at
- reset_token, reset_token_expiry (for password reset)
- manual_spent_adjustment (for admin adjustments)

### cars table
- id, name, brand, category
- price_per_day, image_url, description
- seats, transmission, fuel_type
- is_available, is_featured
- created_at, updated_at

### bookings table
- id, car_id, user_id
- customer_name, customer_email, customer_phone
- cin, driver_license (customer documents)
- start_date, end_date, total_price
- payment_method (cash/card), status (pending/confirmed/completed/cancelled)
- language (fr/ar/en - for emails)
- created_at

### contact_messages table
- id, name, email, phone, message
- status (unread/read/replied)
- created_at

## Key Features Implemented

### 1. Authentication System
- Login/Register with JWT tokens
- Password reset via email with secure tokens (1-hour expiry)
- Dedicated login page at `/login`
- Auth modal for quick access
- Protected routes for admin and user areas

### 2. Multi-Language Support (FR/AR/EN)
- Language switcher in navbar
- All UI elements translated
- Emails sent in user's selected language
- RTL support for Arabic

### 3. Car Management
- Browse cars with filters and search
- Car details page with booking form
- Admin can add/edit/delete cars
- Featured cars on homepage
- Availability status

### 4. Booking System
- Date range picker with validation
- Auto-fill user info (name, email, phone, CIN, driver license) from profile
- Payment method selection (cash/card - both paid at pickup)
- CIN and driver license fields required
- Redirect to "My Bookings" after successful booking
- Users can cancel pending bookings

### 5. Email Notification System (Nodemailer + Gmail SMTP)
**4 Email Types:**
- **Booking Confirmation** - Sent to customer on new booking
- **Admin Alert** - Sent to admin on new booking
- **Status Change** - Sent when admin changes booking status
- **24h Reminder** - Automated cron job sends reminders before pickup

**Email Configuration (.env):**
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=yassineamghar2000@gmail.com
SMTP_PASS=ecdf wgww nxoe akxj
FRONTEND_URL=http://localhost:8080


Cron Scheduler : Runs every hour to check for bookings starting in 24 hours

6. User Profile
View/edit personal info (name, phone, city, CIN, driver license)

View booking history with status tracking

Progress bar showing booking status (pending → confirmed → completed)

7. Admin Dashboard
Analytics : Daily/monthly/yearly revenue, total bookings/users/cars

Car Management : Add/edit/delete cars, manage availability

Booking Management : View all bookings, change status, search/filter

User Management : View users, edit details, track spending

Messages : View contact form submissions

8. Scroll Animations (Framer Motion)
Homepage : Hero section, stats bar with count-up animation, featured cars slide-in

About Page : Staggered fade-in sections

Terms & Privacy : Slide-in sections with delays

Footer : Fade-in on scroll, social icons bounce on hover, links with slide underline

My Bookings : Staggered card animations

Contact Section : Slide-in from left/right

9. UI/UX Enhancements
Glass-morphism design (glass-card class)

Smooth page transitions

Hover effects on cards and buttons

Mobile-responsive design

WhatsApp contact integration

Stats bar with animated counters

API Endpoints
Auth Routes ( /api/auth)
POST /login - User login

POST /register - User registration

POST /forgot-password - Request password reset

GET /validate-reset-token - Validate reset token

POST /reset-password - Reset password with token

Car Routes ( /api/cars)
GET / - Get all cars (with filters)

GET /:id - Get car by ID

POST / - Create car (admin only)

PUT /:id - Update car (admin only)

DELETE /:id - Delete car (admin only)

Booking Routes ( /api/bookings)
GET / - Get all bookings (admin only)

GET /my - Get user's bookings

GET /:id - Get booking by ID

POST / - Create booking

PATCH /:id/status - Update booking status (admin only)

DELETE /:id - Cancel booking

User Routes ( /api/users)
GET /me - Get current user profile

PUT /me - Update current user profile

GET / - Get all users (admin only)

GET /analytics - Get analytics data (admin only)

PUT /:id - Update user (admin only)

Contact Routes ( /api/contact)
POST / - Submit contact form

Environment Variables (.env)

# Supabase (not actively used, legacy)
VITE_SUPABASE_PROJECT_ID=wtuwkzzuatutudbragff
VITE_SUPABASE_PUBLISHABLE_KEY=...
VITE_SUPABASE_URL=https://wtuwkzzuatutudbragff.supabase.co

# Admin Account
ADMIN_EMAIL=admin@drivex.com
ADMIN_PASSWORD=admin1234
ADMIN_FIRST_NAME=sihabi
ADMIN_LAST_NAME=simustapha
ADMIN_PHONE=+21261604965
ADMIN_CITY=agadir

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=yassineamghar2000@gmail.com
SMTP_PASS=ecdf wgww nxoe akxj
FRONTEND_URL=http://localhost:8080


Recent Changes & Current State
Latest Updates:
✅ Added CIN and driver license fields to booking form and user profile

✅ Implemented password reset system with email verification

✅ Added confirm password field to registration

✅ Implemented scroll animations throughout the site

✅ Auto-fill booking form with user profile data (phone, CIN, driver license)

✅ Updated User type to include cin and driver_license fields

✅ Updated backend /users/me endpoints to handle cin and driver_license

Known Issues:
None currently - all features working

Company Information:
Company Name : Sihabi Cars

Email : /

Phone : +212 661 604 965

Address : 132 Ahmed Zakaria Street, Bloc Abtih Extension Dakhla, Agadir, Morocco

Social : Instagram, Facebook, WhatsApp, TikTok

Important Notes
Database file: server/wheelie.db (SQLite)

JWT secret is hardcoded in server/middleware/auth.js

Email system works in "preview mode" (console logs) if SMTP not configured

Payment is NOT processed online - both cash and card are paid at vehicle pickup

Default language is French (fr)

Admin dashboard accessible at /admin

Booking confirmation redirects to /my-bookings page

Next Steps / Potential Features
Car image gallery (multiple images per car)

Car availability calendar

Advanced search filters (price range, transmission, fuel type)

Customer reviews and ratings

Promotional codes/discounts

Insurance and add-ons during booking

Booking modifications

SMS notifications

Current Status : All core features are implemented and working. The application is fully functional with authentication, booking system, email notifications, multi-language support, and admin dashboard.





Project Overview : Full-stack car rental website "Sihabi Cars" for a Moroccan company using React/TypeScript/Vite/Tailwind (frontend) and Node.js/Express/SQLite (backend) with multi-language support (FR/AR/EN)

Car Card Hover Animation : Fixed hover effect on car cards by removing Framer Motion whileHover and using CSS transition-transform with hover:scale-[1.03] for smooth enter/leave animations

Admin Dashboard Navigation : Modernized desktop navigation from flat TabsList to pill-style nav with icons, badges, and active state styling while keeping mobile select dropdown unchanged

Rate Limiting Implementation : Added express-rate-limit to auth routes (login/register) with 10 attempts per 15 minutes per IP+email combination to prevent brute force attacks

Error Alert Styling : Created modern ErrorAlert component in AuthModal that differentiates rate limit errors (amber with Clock icon) from password errors (red with AlertCircle icon)

JWT Secret Configuration : Confirmed JWT_SECRET properly configured in .env with fallback in server/middleware/auth.js

Meta Tags and SEO : Updated index.html with proper Open Graph tags, Twitter cards, and favicon; updated robots.txt to block private pages (/admin, /my-bookings, /profile, /login)

Multilingual Car Descriptions : Implemented database migration adding description_fr, description_ar, description_en columns to cars table; updated admin form with 3 separate description textareas; created getDescription() function in CarDetailsPage to display correct language

Loading Skeletons : Added shimmer animation CSS and replaced pulse placeholders with shaped skeleton cards matching CarCard layout

Translation Completeness : Added missing translation keys for "Charts", "Export CSV", "Car not found", and "Description" across all 3 languages

Database Seeding Removal : Removed all car seeding logic from server/db.js so database starts with 0 cars, only admin account

Instagram Link : Updated Footer component Instagram link to real company profile: https://www.instagram.com/sihabiluxurycars

Files and Code Summary
/goinfre/yaimghar/wheelie-happy-car/src/components/CarCard.tsx: Removed whileHover from motion.div, added CSS class transition-transform duration-300 ease-in-out hover:scale-[1.03] for smooth hover animations

/goinfre/yaimghar/wheelie-happy-car/src/pages/AdminPage.tsx: Replaced TabsList with custom pill navigation on desktop using button elements with icons, labels, and conditional badges; mobile select unchanged; added translation keys for Charts tab

/goinfre/yaimghar/wheelie-happy-car/server/routes/auth.js: Imported { rateLimit, ipKeyGenerator } from express-rate-limit; created authLimiter with 15min window, max 10 attempts, keyGenerator using ipKeyGenerator(req)_${email} for per-user rate limiting; applied to both /login and /register routes

/goinfre/yaimghar/wheelie-happy-car/server/index.js: Added app.set('trust proxy', 1) to enable rate limiter IP detection

/goinfre/yaimghar/wheelie-happy-car/src/components/AuthModal.tsx: Created ErrorAlert component that checks message content for "too many" or "15 minutes" to display amber Clock icon for rate limits vs red AlertCircle for other errors; replaced plain error paragraphs with ErrorAlert component

/goinfre/yaimghar/wheelie-happy-car/.env: Contains JWT_SECRET, admin credentials, SMTP config, FRONTEND_URL; admin password changed to Sihabi@Cars2024!

/goinfre/yaimghar/wheelie-happy-car/index.html: Updated with French-focused meta tags, Open Graph properties with absolute URLs, Twitter cards, proper description and keywords

/goinfre/yaimghar/wheelie-happy-car/public/robots.txt: Updated to allow public pages (/, /cars, /about, /contact) and disallow private pages (/admin, /my-bookings, /profile, /login, /forgot-password, /reset-password)

/goinfre/yaimghar/wheelie-happy-car/server/db.js: Added migration to create description_fr, description_ar, description_en columns if they don't exist; removed all car seeding logic (lines 88-184 deleted) so database starts empty except admin account

/goinfre/yaimghar/wheelie-happy-car/server/routes/cars.js: Updated POST and PUT routes to handle description_fr, description_ar, description_en fields in request body and database operations

/goinfre/yaimghar/wheelie-happy-car/src/lib/supabase-helpers.ts: Updated Car type to include description_fr, description_ar, description_en fields; confirmed no 3-second delay in fetchCars

/goinfre/yaimghar/wheelie-happy-car/src/pages/CarDetailsPage.tsx: Added getDescription(car) function that returns description based on current language with fallback chain (FR → EN → AR → old description); replaced hardcoded "Description" with t('cars.description')

/goinfre/yaimghar/wheelie-happy-car/src/index.css: Added .skeleton class with shimmer animation using CSS keyframes and linear gradient

/goinfre/yaimghar/wheelie-happy-car/src/pages/CarsPage.tsx: Replaced simple pulse div with detailed skeleton cards matching CarCard structure (image area, brand/name, price, specs row)

/goinfre/yaimghar/wheelie-happy-car/src/hooks/use-language.tsx: Added translation keys: admin.chartsTab, admin.exportCSV, cars.notFound, cars.description, admin.calendarTab across all 3 languages (FR/AR/EN)

/goinfre/yaimghar/wheelie-happy-car/src/components/Footer.tsx: Updated Instagram href from https://instagram.com to https://www.instagram.com/sihabiluxurycars

Key Insights
User Preference : User wants minimal code changes and is cautious about features that might "corrupt" code

Rate Limiting Behavior : On localhost all requests come from 127.0.0.1, but on production each user has unique IP; rate limiting by IP+email allows different users to have separate attempt counters

Database Strategy : User wants to start with 0 cars and manually add real cars from their agency via admin dashboard

Deployment Goal : User wants free hosting to test on mobile and share with friends before official launch

Company Details : Sihabi Cars (Sihabi Luxury Cars on Instagram), located in Agadir, Morocco; phone +212 661 604 965; Instagram @sihabiluxurycars

Environment Variables : JWT_SECRET generated, admin password set to Sihabi@Cars2024!, SMTP configured with Gmail app password

Translation Keys : All hardcoded English strings have been replaced with translation keys; Charts tab was last hardcoded element fixed

Most Recent Topic
Topic : Preparing for deployment - user wants free hosting to test website on mobile and share with friends

Progress :

Removed car seeding logic so database starts with 0 cars (only admin account)

Updated Instagram link to real company profile

All translations completed

Site is functionally ready for deployment

User Request : Asked for free hosting recommendations to deploy and test the full-stack app (React frontend + Node.js backend + SQLite database)

Recommendation Provided : Suggested Render.com as best free option with:

Free tier supporting Node.js + SQLite

Auto-deploy from GitHub

HTTPS included

Custom domain support

Caveat: sleeps after 15min inactivity (30sec wake time)

Alternative options: Railway.app ($5 credit/month) or split deployment (Vercel frontend + Render backend)

Next Step : User needs to decide if they want help preparing code for Render deployment (requires adding config files)